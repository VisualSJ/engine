'use stirct';

const dock = require('@editor/dock');

class Layout {
    
    /**
     * 应用布局
     * @param {*} layout 
     */
    apply (layout) {
        // todo 发送给 main window
    }

}

module.exports = new Layout();